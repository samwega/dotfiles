auto-reload yes

reload-time 7

browser "brave %u"

#reload-threads 10 # for updating in parallel, adjust number

#bind keys
bind-key h quit
bind-key BACKSPACE quit
bind-key j down
bind-key k up
bind-key l open
bind-key H prev-feed
bind-key L next-feed
bind-key c toggle-show-read-feeds
